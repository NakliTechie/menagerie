# Menagerie v1.2 — "It's a tree" — build spec

> **Lock-and-fold gate.** This is the spec the v1.2 build runs off, the way v1.1
> ran off `HANDOFF-v1.0.md` + the §7 UX reference. No tree code lands until this
> and [`v1.2-ux-reference.md`](v1.2-ux-reference.md) are locked. Where the doc and
> the repo disagree during build, the repo wins — record the divergence in `STATE.md`.

## 1. What v1.2 is

The deferred supervisor-tree surface: a session can **spawn child sessions**, and
Menagerie shows the parent→children relationship as a **collapsible tree**, with
**subtree operations** (collapse, kill-subtree). The structured transport (v1.1)
is the load-bearing feature; the tree is the research surface — so v1.2 ships the
*smallest tree that is genuinely useful* and defers the speculative parts.

**Success bar (from `DEFERRED.md`):** one supervisor session spawns 4 worker
children; all five are visible as one collapsible tree; killing the supervisor
kills the subtree.

### In scope (v1.2.0)
1. **Parent-child linkage** — a session spawned *by* another session records its
   parent; the relay reports it.
2. **Tree view** — a toggle between the current flat grid and a tree that nests
   children under their parent (sidebar tree + grid grouping).
3. **Subtree kill** — killing a parent offers to kill its whole subtree.
4. **Re-attach rebuilds the tree** — parentage survives a refresh / relay restart.

### Deferred to a later v1.2.x (out of this spec)
- **Agent-to-agent coordination layer** — `wait`, atomic prompt+wait, agent self-
  reported status. The tree (v1.2.0) makes parentage *visible*; the coordination
  layer makes a supervisor *act on* it. Specced in §8 so the v1.2.0 protocol doesn't
  paint it into a corner; built after the tree UI lands. **Directly informed by herdr**
  (`herdr.dev/docs/socket-api`, 32.8K★) — see the capture `[[2026-03-27-herdr]]`.
- **Cross-relay child-spawn** — a child on a *different* relay than its parent.
  Needs a parentage reference that spans relays; the WS/token model is per-relay.
  Design it once single-relay trees are real.
- **Multi-workspace tabs** — independent of the tree research; its own spec.
- **Tiny agent-client lib** — "the protocol is the SDK" holds for v1.2; a wrapper
  only if a supervisor-agent author asks.

### The parentage model (the load-bearing decision — D-v1.2-1)
**A child is a menagerie session spawned with a declared parent — not an OS
subprocess the relay infers.** A supervisor spawns a child by sending a `spawn`
frame carrying `parent_session_id`; the relay records parentage and emits
`child_spawned`. Rationale: explicit, reconstructable on re-attach, and forward-
compatible with cross-relay trees; process-tree sniffing is fragile, doesn't cross
relays, and a child spawned out-of-band isn't a menagerie session anyway (no tile,
no stream). This matches the existing `child_spawned`/`child_session_id` seam and
the "protocol is the SDK" stance (§16) — a supervisor agent (orchestrator) drives
child spawns over the same WS it's streamed on.

**Parentage is a hint, not an ownership assertion** (forward-pass 2026-08-27 relay-L3).
`validParent` checks that the named parent is live, not that the caller owns it, so a
client can graft a session under another's. That escalates nothing: a single global
registration token plus tokenless re-attach already mean one trust domain with full
control. Do not read the tree as an authorization boundary.

## 2. Protocol delta (→ 1.3, additive per §8)

Bump `PROTOCOL_VERSION` `1.2` → `1.3` in **both** `protocol/types.ts` and
`relay-go/internal/protocol/messages.go` (D4 precedent: an added field is a minor
bump). All fields optional; a 1.2 client/relay ignores them and sees a flat grid.

- **`Spawn`** gains `parent_session_id?: string`. When set, the relay verifies the
  parent is a live session owned by the same registration and records parentage.
  An unknown/dead parent → spawn proceeds as a root session (log a warning) — never
  fail a spawn over a stale parent.
- **`Spawned`** echoes `parent_session_id?: string` so the client links the new tile
  immediately (not only via the event).
- **`Event{child_spawned}`** (already defined) is emitted to the **parent's** owner
  when a child spawns: `{session_id: <parent>, event: "child_spawned",
  child_session_id: <child>}`. The child ALSO surfaces as a normal `spawned`/session
  to its own owner (usually the same connection).
- **`SessionInfo`** (the re-attach descriptor) gains `parent_session_id?: string` so
  a refresh reconstructs the tree. (v1.1 already added `transport` here — same shape.)
- **`hello.hosts_children`** becomes `true` (was hardcoded `false`). A client only
  offers "spawn child" / tree affordances against a relay that advertises `true`.
- **Subtree kill** — extend `Signal`: `{type:"signal", signal:"kill", subtree?:true}`.
  With `subtree:true` the relay kills the session and every descendant (depth-first,
  children before parents), emitting an `exited` per session. No new frame type.

### Invariants
- Parentage is a **DAG-free tree**: reject a `parent_session_id` that would create a
  cycle (a session can't be its own ancestor). O(depth) walk on spawn.
- A child's lifecycle is independent unless subtree-killed: a parent exiting does
  **not** auto-kill children (a supervisor can hand off long-running workers) — the
  UI *offers* subtree kill, never forces it.
- Re-attach: the relay reports `parent_session_id` in `listSessions`; the client
  rebuilds the tree from those, tolerant of a parent that's already exited (orphan →
  promoted to root, shown with a "parent exited" marker).

## 3. Relay changes (`relay-go`)

- `sessionEntry` gains `parent string` (guarded by `s.mu`, like the rest of the map).
- `handleSpawn`/`handleSpawnACP`: read `msg.ParentSessionID`; validate (live, same
  registration, no cycle) under `s.mu`; store on the entry; echo in `Spawned`; queue
  a `child_spawned` Event to the parent.
- `listSessions`: include `parent_session_id`.
- Subtree kill: a `killSubtree(id)` that snapshots descendants under `s.mu` (a
  parent→children index or a scan of entries by `parent`), then kills leaf-first.
  Reuse the existing per-session kill; each still emits `exited`.
- `child_spawned` rides the **same outbox/trySend funnel** as every other frame
  (Batch A's closed-guard applies) — never a raw `e.outbox <-`.
- `hosts_children = true` in the hello builder.

## 4. Browser changes (`index.html`)

- **Session model:** each session record gains `parentId` (from `spawned`/`SessionInfo`).
  A derived `childrenOf(id)` index, rebuilt on session add/remove.
- **Tree view toggle** (topbar, next to the sidebar toggle): flat grid ⇄ tree. Persist
  the choice (localStorage) like the theme.
- **Sidebar tree:** sessions render as a collapsible tree (parent with a disclosure
  triangle, children indented). Collapse state per-parent, persisted.
- **Grid grouping (tree view on):** children render grouped under their parent (an
  indented band or a bounding group), not scattered in spawn order.
- **Spawn-child affordance:** on a tile whose relay `hosts_children`, a "spawn child"
  action pre-fills the spawn dialog with `parent_session_id`.
- **Subtree kill:** the tile/parent kill control, when the session has children, asks
  (in-app modal — NOT `confirm()`, per the no-native-dialogs rule) "kill this session
  and its N children?" → sends `signal{kill, subtree:true}`.
- **Re-attach:** rebuild `parentId` from `SessionInfo.parent_session_id`; render the
  tree; orphaned children (parent gone) show a "parent exited" chip and sit at root.
- All new agent/relay-controlled strings through `escapeHtml` (H1 discipline).

## 5. Build order (chunks — each ends green: `go test -race`, lint, browser check)

- **C0 — protocol 1.3 + fixtures.** Bump version; add the optional fields; add a
  fixture per new/changed frame; `validate-fixtures.mjs` green. No behavior yet.
- **C1 — relay parentage.** `parent` on the entry, spawn validation (live/same-reg/
  no-cycle), `Spawned` echo, `child_spawned` event, `listSessions` field,
  `hosts_children=true`. Unit tests: spawn-with-parent, cycle-rejected, child_spawned
  emitted, re-attach carries parent.
- **C2 — relay subtree kill.** `signal{kill, subtree}` kills leaf-first; test: parent
  + 2 children, subtree kill → 3 `exited`, leaf-before-parent order.
- **§7 UX gate — `v1.2-ux-reference.md` locked** (tree rendering, collapse, grouping,
  spawn-child, subtree-kill modal, empty/orphan states). C3+ don't open until it's set.
- **C3 — browser tree model + sidebar tree.** parentId, childrenOf index, tree toggle,
  collapsible sidebar tree, persisted collapse. Browser-verified via the demo seam
  (extend `fakeAcpSession` with a parent/child pair).
- **C4 — grid grouping + spawn-child + subtree-kill modal.** Children grouped in the
  grid under tree view; spawn-child pre-fill; in-app subtree-kill confirm.
- **C5 — re-attach rebuilds the tree.** parentId from SessionInfo; orphan handling;
  verify a refresh reconstructs the tree and a mid-tree drop restores it.
- **C6 — close-out.** README/VISION amend (v1.2 shipped), STATE.md, a fresh-context
  `/forward-pass` over the branch before merge (the C6 gap we won't repeat).

## 6. Gate artifacts (what "done" means)
- `go build ./... && go vet ./... && go test -race ./...` green, incl. new parentage +
  subtree tests.
- Lint gate clean (index.html + any generated file).
- Browser proof: the success bar — 1 supervisor + 4 children as a collapsible tree,
  subtree kill removes all five — captured via the demo seam (no live orchestrator
  needed) and, if an orchestrator agent is available, once end-to-end.
- Protocol 1.3 fixtures validate; `PROTOCOL_VERSION` matches across types.ts +
  messages.go (the drift check from the last forward pass).

## 7. Open questions (decide before the chunk that needs them)
- **Q1 (before C4):** grid grouping — indented band vs. a bounding card vs. tree-only-
  in-sidebar (grid stays flat)? → the §7 UX reference decides; lean sidebar-tree-first,
  grid grouping as a lighter indent.
- **Q2 (before C1):** does a child inherit the parent's cwd/agent by default in the
  spawn-child pre-fill, or start blank? Lean: pre-fill cwd from parent, agent blank.
- **Q3 (deferred, not blocking):** cross-relay trees — parkuntil single-relay ships.

## 8. Coordination layer (v1.2.x — specced now, built after the tree UI)

The tree makes parentage *visible*; coordination lets a supervisor session **act on**
it — spawn a worker, then *park until it genuinely needs you*, without polling. Ported
from herdr's socket API (`herdr.dev/docs/socket-api`, capture `[[2026-03-27-herdr]]`),
mapped onto Menagerie's model: **an agent is just another WS client** (§16, "the protocol
is the SDK"), so coordination is new *frames*, not a side-channel. Nothing here is
needed for v1.2.0 — it's specced now only so the 1.3 frames don't box it in.

### 8.1 The `wait` frame (the load-bearing primitive)
`{type:"wait", session_id, session_token, until, timeout_ms?}` where `until` is one or
more of `{needs_input, idle, exited}` (Menagerie's existing status vocabulary — not new
states). The relay resolves it **event-driven** against the target session's next
matching status transition (it already emits `needs_input`/`idle`/`exited` events),
replying `{type:"waited", session_id, state, timed_out}`. Semantics ported from herdr's
`agent.wait`:
- **Server-owned + event-driven** — no client polling; the relay resolves on the
  transition it already tracks. Reuses the status machinery, not a new watcher.
- **Occupant-pinning** — pin the resolved session by `session_id`; a re-attach keeps
  the same id, but a *different* session reusing a slot must not satisfy a stale wait.
  (Menagerie ids are stable + tokened, so this is lighter than herdr's pane-occupant
  race, but the invariant holds: a wait resolves only for the session it named.)
- **Timeout** — `timeout_ms` bounds it; on expiry reply `timed_out:true`. No infinite
  server-side waits (the C1 concurrency lessons apply — a waiter must never leak;
  resolve-or-timeout, drain on session exit like the pending-request fix).

#### 8.1.1 Edge cases (folded in 2026-09-07 from herdr v0.8.2's documented behaviour)
herdr shipped this primitive first and its docs state the edges it hit. Taking them as
spec input costs an hour and saves finding each one in production. Each is written here
as a Menagerie rule, not a herdr quotation.

- **E1 — a wait whose condition is already true resolves immediately.** `wait` observes
  the target's *current* status before arming, and returns at once if it already matches.
  Without this, a supervisor that spawns a worker, misses the transition by a millisecond,
  and then waits, parks until timeout on a condition that already happened.
- **E2 — the relay does not track turns.** If the target is already `working` when the
  wait arms, the completion of *that* turn satisfies it — not necessarily the work the
  caller had in mind. This is a real ambiguity, not an implementation gap: the relay
  cannot tell one turn from the next without agent cooperation. Document it in
  `protocol.md`, and note that `report_status` (§8.3) is the way an agent removes it.
- **E3 — `unknown` never satisfies a wait implicitly.** When the lifecycle state is
  added (pending.md), a wait resolves on `unknown` only if the caller named it in
  `until`. An unclassifiable agent is not a finished one, and a wait that resolves on
  "I can't tell" is worse than a timeout, which at least says so.
- **E4 — session exit always resolves every waiter.** On exit the relay drains waiters
  with `{state:"exited"}` rather than letting them run to timeout. A waiter must never
  outlive its session (the M4 lesson, already in §8.1's timeout rule — stated here as the
  exit case too).
- **E5 — `done` vs `idle` are distinct wait targets.** Once the seen/unseen split lands
  (pending.md), `done` = finished and not yet looked at; `idle` = finished and seen. A
  supervisor almost always wants `done`. Reading a session over the protocol must not
  clear `done` — only real attention in the UI does. Otherwise a supervisor that polls
  its workers silently marks them all as seen and the human's attention badge goes quiet.

### 8.2 Atomic prompt+wait
`prompt` gains an optional `wait?: {until, timeout_ms}`: submit the prompt **and** arm
the wait in one frame, so a supervisor can't miss a transition that happens between a
separate prompt and wait (herdr's stated race). The relay arms the wait before
dispatching the prompt.

#### 8.2.1 Edge cases (same fold)
- **E6 — never send input to a blocked session.** If the target is already
  `needs_input`, `prompt` returns `{error: "session_blocked"}` and sends **nothing**.
  This is the sharpest of the edges: an approval dialog reads the next keystrokes as its
  answer, so a supervisor's prompt text would *approve or reject a tool call it never
  saw*. The caller must inspect the pending request and answer it deliberately —
  `permission_response` for ACP, explicit `input` for PTY.
  - **Menagerie caveat (ours, not herdr's):** we know `needs_input` *authoritatively*
    for ACP (a real `session/request_permission` is pending) but only *heuristically*
    for PTY (`LooksLikeNeedsInput`). So the guard is a hard refusal on ACP and a
    best-effort refusal on PTY — and it must be documented that way rather than implying
    a guarantee the PTY path cannot make. A PTY false-negative can still land text in a
    dialog; that is a heuristic problem, and the honest fix is §8.3.
- **E7 — ~~an armed wait that observes nothing must fail loudly~~ REVISED 2026-09-07
  during the build.** The original rule was: after an accepted prompt, see a lifecycle
  change within a 5s grace window or reply `prompt_stalled`. **It does not apply to
  Menagerie's prompt path, and implementing it produced a false positive on the common
  case.**
  - Why it holds for herdr: it submits work by *typing keystrokes into a TUI*. Those
    can vanish into an application that was not listening, so silence afterwards is
    real evidence the prompt never landed.
  - Why it fails for us: our only task-submission path is ACP, a framed JSON-RPC
    channel. Once `Prompt` returns without error the request is on the agent's stdin.
    The remaining failure — an agent that accepts and never answers — is exactly what
    the wait's own timeout reports. Layering a stall timer on top mislabels a slow
    agent, because an agent that merely *thinks* for longer than the window emits
    nothing either. The test that proved it (`TestPromptSlowAgentIsNotAStall`) failed
    against the implemented guard and passes with it removed; a swallowed prompt is
    covered instead by `TestPromptSwallowedByTheAgentTimesOut`.
  - Standing rule: **no stall guard on the ACP path.** If a PTY prompt frame is ever
    added, the guard belongs there, where keystrokes genuinely can go nowhere.
- **E8 — a caller's timeout is always honoured as a timeout.** Expiry replies
  `timed_out:true` with the state as it stands. (Originally phrased against the stall
  window E7 introduced; with no stall guard on this path, it is simply the rule.)

### 8.3 Agent self-reported status (`report_status`)
`{type:"report_status", session_id, session_token, state, message?}` where `state ∈
{working, needs_input, idle, done}`. Today Menagerie *derives* status (PTY heuristics
+ ACP events); a self-report lets an agent **declare** its state authoritatively — more
reliable than the `LooksLike*` heuristics (whose false-positives the last forward pass
flagged, L5). Display-only metadata (title, token counts) stays separate and never
drives a `wait` resolution (herdr's `report_metadata` vs `report_agent` split).

- **E9 — one status authority per session, never two.** If a session reports its own
  status, the relay stops applying `LooksLike*` heuristics to it for the duration; it
  does not merge the two. herdr is explicit about this and the reason is sound: two
  sources of truth produce a state that flickers between them, and neither can be
  debugged. Authority is per-session and lasts until the session exits.

### 8.3.1 Cross-relay waits are brokered by a client (decided 2026-09-07)
A relay resolves a wait only for a session **it hosts**, and never pretends to own
one it cannot see. A wait spanning relays is therefore composed by a client: one
relay-local `wait` per session, joined into a single result carrying
`brokered: true`.

- **The flag is the honesty.** A relay-owned wait has the relay's durability; a
  brokered one lives exactly as long as its broker. Marking it is how a supervisor
  tells them apart instead of assuming.
- **The browser brokers today** (`window.__menagerie.wait({sessions, until, mode})`,
  `mode: "any" | "all"`). An agent-side broker later runs the same composition over
  its own relay connections — same logic, no protocol change, which is exactly why
  the frames stay relay-local.
- **A relay that drops resolves its half as `unreachable`, never hangs.** A broker
  that can no longer see a session must say so; silence would be the failure this
  layer exists to prevent.
- **Known limitation, stated rather than hidden:** brokered waits die with the tab.
  That is a real gap against Menagerie's own model, where sessions are server-owned
  and outlive the connection. It closes when the agent-side broker lands, not before.

### 8.4 Events already covered
herdr's `events.subscribe` is largely free here: a supervisor **agent-client** already
receives every session's `event` frames over its WS (the protocol is the SDK). A
per-session/per-state *filter* is a nicety, not a new capability — defer unless a
supervisor author asks.

### 8.5 What this unlocks (the herdr workflow, on Menagerie's rails)
Supervisor spawns N workers (`spawn{parent_session_id}`, v1.2.0) → `prompt{wait:{until:
"needs_input"}}` each → parks until a worker is genuinely blocked → **inspects the
pending request and answers it deliberately** (E6: never re-prompt a blocked session) →
repeats;
subtree-kill (v1.2.0) tears the whole thing down. The tree UI *shows* it; §8 lets an
agent *drive* it headless.

## Decisions log (this build)
- **D-v1.2-1 — parentage is protocol-declared, not process-inferred** (§1). A child is
  a menagerie session spawned with `parent_session_id`; the relay never sniffs the OS
  process tree.
- **D-v1.2-3 — the coordination layer refuses rather than guesses** (§8.1.1, §8.2.1,
  added 2026-09-07). A wait never resolves on `unknown`; a prompt never sends input to a
  blocked session; an armed wait that observes nothing fails loudly. Where a guard can
  only be best-effort (PTY blocked-detection is heuristic), the spec says so instead of
  implying a guarantee.
- **D-v1.2-5 — an edge case ported from another product must survive contact with our
  own architecture** (§8.2.1 E7, 2026-09-07). The stall guard was specced from herdr,
  implemented, and then removed when a test showed it mislabelled every slow agent: the
  failure mode it guards is specific to blind keystroke injection, and our path is an
  RPC. Folding in someone else's hard-won edges is cheap; adopting them unexamined is
  not.
- **D-v1.2-4 — one status authority per session** (§8.3, E9). Self-reported status
  displaces the heuristics for that session rather than merging with them; two sources
  of truth flicker and cannot be debugged.
- **D-v1.2-2 — coordination is WS frames, not a side-channel** (§8). `wait` / atomic
  prompt+wait / `report_status` are first-class protocol frames a supervisor agent-client
  sends over the same connection it's streamed on — herdr's socket-API model, mapped onto
  "the protocol is the SDK." Specced as v1.2.x; the 1.3 shapes leave room for it.
